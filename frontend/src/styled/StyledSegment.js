import styled from 'styled-components';
import { Segment } from 'semantic-ui-react';

export const StyledSegmentWrapper= styled(Segment)({
    width: '75vw', 
    marginLeft: 'auto !important', 
    marginRight: 'auto !important',
    background: '#1b1c1d !important',
    marginTop: '0 !important',
})

export const StyledSegmentTablet = styled(Segment)({
    padding: '1.5em 0em',
    width: '75vw', 
    marginLeft: 'auto', 
    marginRight: 'auto',
})

export const StyledSegmentDeskTop = styled(Segment)({
    padding: '1.5em 0em',
    width: '75vw', 
    marginLeft: 'auto', 
    marginRight: 'auto'
})

